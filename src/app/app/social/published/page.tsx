import { ModulePage } from "@/components/layout/module-page";

export default function Page() {
  return (
    <ModulePage
      title="Published posts"
      description="History of successful publishes with account, time, and performance placeholders."
      bullets={['Phase 2 fills this from platform APIs.']}
    />
  );
}
