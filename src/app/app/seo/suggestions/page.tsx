import { ModulePage } from "@/components/layout/module-page";

export default function Page() {
  return (
    <ModulePage
      title="Content suggestions"
      description="AI topics mapped to keyword gaps and calendar slots."
      bullets={['Generate from SEO + brand profile.']}
    />
  );
}
