import { ModulePage } from "@/components/layout/module-page";

export default function Page() {
  return (
    <ModulePage
      title="Page optimization"
      description="On-page briefs for title, H1, internal links, and schema."
      bullets={['Fed by the latest audit.']}
    />
  );
}
