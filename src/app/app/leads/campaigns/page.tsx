import { ModulePage } from "@/components/layout/module-page";

export default function Page() {
  return (
    <ModulePage
      title="Lead campaigns"
      description="Capture forms, magnets, and follow-up sequences."
      bullets={['CRM statuses are live on the Leads page.']}
    />
  );
}
