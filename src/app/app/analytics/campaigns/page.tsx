import { ModulePage } from "@/components/layout/module-page";

export default function Page() {
  return (
    <ModulePage
      title="Campaign performance"
      description="Channel contribution and KPI tracking."
      bullets={['Filled by campaign + ad integrations.']}
    />
  );
}
