import { ModulePage } from "@/components/layout/module-page";

export default function Page() {
  return (
    <ModulePage
      title="Campaigns"
      description="Name, goal, audience, channels, budget, dates, and AI recommendations."
      bullets={['Builder is ready; ad-network publish is Phase 4.']}
    />
  );
}
