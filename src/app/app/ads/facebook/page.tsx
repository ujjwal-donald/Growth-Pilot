import { ModulePage } from "@/components/layout/module-page";

export default function Page() {
  return (
    <ModulePage
      title="Facebook ads"
      description="Meta campaign objects once marketing API credentials are present."
      bullets={['Uses the shared ad generator for copy.']}
    />
  );
}
