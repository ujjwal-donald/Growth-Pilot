import { ModulePage } from "@/components/layout/module-page";

export default function Page() {
  return (
    <ModulePage
      title="Brand voice"
      description="Tone, examples, words to use and avoid."
      bullets={['The assistant injects this into every prompt.']}
    />
  );
}
